/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InterestSetupSelectorComponent } from './interest-setup-selector.component';

describe('Component: InterestSetupSelector', () => {
  it('should create an instance', () => {
    let component = new InterestSetupSelectorComponent();
    expect(component).toBeTruthy();
  });
});
