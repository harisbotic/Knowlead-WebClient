import { FeedbackFormDirective } from './feedback-form.directive';

describe('FeedbackFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedbackFormDirective();
    expect(directive).toBeTruthy();
  });
});
